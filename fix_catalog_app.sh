sed -i 's/let app = setup_test_app(pool.clone()).await;/let config = std::sync::Arc::new(noko_rs::config::Config {\n        database_url: std::env::var("DATABASE_URL").unwrap(),\n        bind_addr: "127.0.0.1:0".to_string(),\n        auth_mode: "dev_header".to_string(),\n        nocodb_service_token: None,\n        nocodb_service_actor_id: None,\n        db_tx_max_retries: 2,\n    });\n    let app = noko_rs::app::build_router(noko_rs::app::AppState { pool: pool.clone(), config });/' tests/catalog.rs
